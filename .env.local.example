# Anthropic API key for the chaitanya.agent chat window (/api/chat).
# Copy this file to .env.local and fill in your key.
# Without it the chat falls back to an "agent offline" message; everything else works.
ANTHROPIC_API_KEY=sk-ant-...
